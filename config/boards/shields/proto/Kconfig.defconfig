if SHIELD_PROTO_LEFT

config ZMK_KEYBOARD_NAME
    default "Proto"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_PROTO_RIGHT

config ZMK_KEYBOARD_NAME
    default "Proto_R"

endif

if SHIELD_PROTO_LEFT || SHIELD_PROTO_RIGHT

config ZMK_SPLIT
    default y

endif
